---
// Admin panel page that loads Decap CMS
// IMPORTANT: Replace YOUR_NETLIFY_SITE_URL with your actual Netlify site URL
// Example: https://your-site-name.netlify.app
// Find this in your Netlify dashboard under your site's URL
const netlifySiteUrl = 'https://serene-rugelach-9ae7b9.netlify.app';
---

<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Content Manager</title>
    <!-- Netlify Identity widget - must load from your Netlify site to avoid CORS -->
    <script src={`${netlifySiteUrl}/.netlify/identity`}></script>
  </head>
  <body>
    <!-- Include the script that builds the page and powers Decap CMS -->
    <script src="https://unpkg.com/decap-cms@^3.0.0/dist/decap-cms.js"></script>
    <script>
      // Initialize Netlify Identity
      if (window.netlifyIdentity) {
        window.netlifyIdentity.on('init', (user) => {
          if (!user) {
            window.netlifyIdentity.on('login', () => {
              document.location.href = '/WebTemplate/admin/';
            });
          }
        });
      }
    </script>
  </body>
</html>
